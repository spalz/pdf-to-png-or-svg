# Claude Code - PDF Extraction

Follow `../WORKFLOW.md` first. This repo contains Python PDF extraction scripts and large generated outputs.

Do not mass-regenerate images/PDF-derived outputs without explicit request. Use a small sample only when verification is needed.
